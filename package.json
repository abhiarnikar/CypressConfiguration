{
  "name": "cyconfig",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "stag:config":"npx cypress run --headed --config-file staging-config.json",
    "prod:config":"npx cypress run --headed --config-file production-config.json",
    "local:config":"npx cypress run --headed --config-file local-config.json",
    "prod:config1":"npx cypress run --headed --env ENV=production",
    "stag:config1":"npx cypress run --headed --env ENV=staging",
    "qa:config":"npx cypress run --headed --env ENV=qa"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^9.7.0",
    "npx": "^10.2.2"
  }
}
